# =============================================================================
# Why Should I Pay For ElevenLabs - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================

# Storage type: 'local' or 's3'
STORAGE_TYPE=local

# File auto-deletion time in seconds (default: 3600 = 1 hour)
FILE_MAX_AGE_SECONDS=3600

# Local storage directory (only used when STORAGE_TYPE=local)
# LOCAL_STORAGE_DIR=/path/to/audio/files

# =============================================================================
# AWS S3 CONFIGURATION (only needed when STORAGE_TYPE=s3)
# =============================================================================

# S3 bucket name (required for S3 storage)
# S3_BUCKET_NAME=your-bucket-name

# AWS region (default: us-east-1)
# S3_REGION=us-east-1

# S3 key prefix for audio files (default: audio/)
# S3_PREFIX=audio/

# AWS credentials (optional - can use IAM roles instead)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# For production, add your domain:
# CORS_ORIGINS=https://yourdomain.com,http://localhost:5000

# =============================================================================
# EMBEDDING CONFIGURATION
# =============================================================================

# Base URL for the app (used when hosted on subdomain/subpath)
# Leave empty for root hosting
# BASE_URL=https://tts.yourdomain.com
BASE_URL=

# Allow iframe embedding (default: true)
EMBED_ALLOWED=true

# Allowed parent origins for iframe embedding
# Use space-separated list, or * for all origins
# EMBED_PARENT_ORIGINS=https://yourdomain.com https://another.com
EMBED_PARENT_ORIGINS=*

# =============================================================================
# S3 LIFECYCLE POLICY NOTE
# =============================================================================
# For S3 storage, you should also set up a lifecycle policy in AWS Console:
# 1. Go to S3 > Your Bucket > Management > Lifecycle rules
# 2. Create rule: Delete objects after 1 day (or your preferred time)
# 3. Apply to prefix: audio/
# This provides an additional safety net for file cleanup.
