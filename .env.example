# Production environment configuration
# Copy this file to .env and fill in your values

# Server configuration
PORT=5000
FLASK_ENV=production

# Optional: Set to 'true' to enable offline voices (Windows only)
ENABLE_OFFLINE_VOICES=false

# Optional: Maximum text length for TTS (default: 5000)
MAX_TEXT_LENGTH=5000
